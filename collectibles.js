class Collectibles {
  constructor(x, y, width, height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.collected = false; // To track if the collectible is collected
  }
}
